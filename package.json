{
  "name": "manthan-creator-suite",
  "private": true,
  "packageManager": "pnpm@9.4.0",
  "version": "0.2.0",
  "scripts": {
    "dev:web": "pnpm --filter @manthan/web dev",
    "build:web": "pnpm --filter @manthan/web build",
    "dev:api": "cd apps/api && uvicorn main:app --host 0.0.0.0 --port 8081",
    "lint": "eslint ."
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
